# Railway Environment Variables Example
# Copia questi valori nelle variabili d'ambiente su Railway
# NON committare questo file con valori reali!

# ============================================
# BACKEND SERVICE VARIABLES
# ============================================

# Database URL (aggiunto automaticamente da Railway quando colleghi il database)
# Se non lo fa, aggiungi manualmente dal database service
DATABASE_URL=postgresql://user:password@host:port/database?schema=public

# Port (assegnato automaticamente da Railway)
PORT=3001

# CORS Origin (URL del frontend su Railway)
# Sostituisci <your-frontend-url> con l'URL reale del frontend
CORS_ORIGIN=https://your-frontend-service.up.railway.app

# Wheel Secret (genera un secret sicuro con: openssl rand -hex 32)
WHEEL_SECRET=your-secure-random-secret-here

# Node Environment
NODE_ENV=production

# ============================================
# FRONTEND SERVICE VARIABLES
# ============================================

# API URL (URL del backend su Railway)
# Sostituisci <your-backend-url> con l'URL reale del backend
NEXT_PUBLIC_API_URL=https://your-backend-service.up.railway.app

# Node Environment
NODE_ENV=production

