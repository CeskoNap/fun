# Railway configuration for Fun Gaming Platform
# This file configures Railway services for monorepo deployment

[build]
builder = "nixpacks"

# Backend service will be configured in Railway dashboard:
# - Root Directory: backend
# - Build Command: pnpm install && pnpm run build
# - Start Command: pnpm run start:prod
# - Health Check Path: /health

# Frontend service will be configured in Railway dashboard:
# - Root Directory: frontend  
# - Build Command: pnpm install && pnpm run build
# - Start Command: pnpm run start

# PostgreSQL service will be added from Railway's template

